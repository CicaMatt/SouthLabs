/* Shared section-grid layout constants. */

/* Fallback grid cell size (px) used when an element's `--section-grid-size`
   custom property is unavailable. Mirrors the CSS default. */
export const DEFAULT_SECTION_GRID_SIZE = 72;
